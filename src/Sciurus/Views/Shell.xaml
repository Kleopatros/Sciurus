<Window x:Class="Sciurus.Views.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:v="clr-namespace:Sciurus.Views"
    xmlns:vm="clr-namespace:Sciurus.ViewModels"
    Title="{Binding Title}" Width="{Binding Width}" Height="{Binding Height}">
  <Window.Resources>
    <!--<DataTemplate DataType="{x:Type vm:PlaceholderViewModel}">
      <v:PlaceholderView />
    </DataTemplate>-->
  </Window.Resources>

  <DockPanel>
    <!--<ContentControl Content="{Binding SelectedPlaceholderViewModel}" />-->
  </DockPanel>
</Window>
