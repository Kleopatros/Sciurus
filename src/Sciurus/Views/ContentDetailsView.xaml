<UserControl x:Class="Sciurus.Views.ContentDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!--<UserControl.Resources>
    <DataTemplate DataType="{x:Type vm:PlaceholderViewModel}">
      <v:PlaceholderUserControlView />
    </DataTemplate>
  </UserControl.Resources>-->
  
  <Border BorderThickness="1" BorderBrush="Black">
    <TextBlock Text="ContentDetailsView" HorizontalAlignment="Center" VerticalAlignment="Center" />
    <!--<ContentControl Content="{Binding SelectedPlaceholderViewModel}" /> -->
  </Border>
</UserControl>
